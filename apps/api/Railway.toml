[railway]
  builder = "nixpacks"
  language = "node"

[build]
  builder = "nixpacks"

[nixpacks]
  NODE_VERSION = "20"
  INSTALL_CMD = "cd apps/api && npm install"
  BUILD_CMD = "cd apps/api && npx prisma generate && npm run build"
  START_CMD = "cd apps/api && npm run start:prod"

[deploy]
  healthcheckPath = "/health"
  healthcheckPort = 4000
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
  numReplicas = 1
